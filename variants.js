function axisModel(product){const variants=product.variants.filter(v=>v.axes?.soft&&v.axes?.hard);if(variants.length<2)return null;const unique=type=>[...new Map(variants.map(v=>[v.axes[type].label,v.axes[type]])).values()],soft=unique('soft'),hard=unique('hard');return soft.length>1||hard.length>1?{variants,soft,hard}:null}
function axisVariant(product,variant){return axisModel(product)&&(!variant.axes?.soft||!variant.axes?.hard)?axisModel(product).variants[0]:variant}
function axisControls(product,variant){const model=axisModel(product);if(!model)return'';variant=axisVariant(product,variant);const group=(type,title,shape)=>`<div class="axis-group"><div class="variant-label"><strong>${title}</strong><span>${escapeHtml(variant.axes[type].label)}</span></div><div class="axis-options">${model[type].map(option=>{const other=type==='soft'?'hard':'soft',available=model.variants.some(v=>v.axes[type].label===option.label&&v.axes[other].label===variant.axes[other].label);return`<button class="axis-swatch ${shape} ${variant.axes[type].label===option.label?'active':''}" style="--axis-color:${option.hex}" data-axis="${type}" data-axis-value="${escapeAttr(option.label)}" data-product="${escapeAttr(product.id)}" title="${escapeAttr(option.label)}" aria-label="${escapeAttr(title+': '+option.label)}" ${available?'':'disabled'}></button>`}).join('')}</div></div>`;return`<div class="dual-axes">${group('soft','Цвет обивки или подушки','soft-axis')}${group('hard','Цвет каркаса или основания','hard-axis')}</div>`}
function selectAxis(product,type,value){const current=axisVariant(product,currentVariant(product,product._selected)),other=type==='soft'?'hard':'soft',match=product.variants.find(v=>v.axes?.[type]?.label===value&&v.axes?.[other]?.label===current.axes?.[other]?.label);if(match)product._selected=match.sourceId;return match}
function cardTemplate(product){let variant=axisVariant(product,currentVariant(product,product._selected));product._selected=variant.sourceId;return`<article class="product-card"><div class="product-visual" data-open="${escapeAttr(product.id)}" tabindex="0" role="button"><img src="${escapeAttr(imageUrl(variant.primaryImage))}" alt="${escapeAttr(stripModel(product.name))}" loading="lazy"><button class="favorite ${state.favorites.includes(product.id)?'active':''}" data-favorite="${escapeAttr(product.id)}">${state.favorites.includes(product.id)?'♥':'♡'}</button>${product.variants.length>1?`<span class="variant-badge">${product.variants.length} ${plural(product.variants.length,'вариант','варианта','вариантов')}</span>`:''}</div><div class="product-info"><h3 class="product-name">${escapeHtml(stripModel(product.name))}</h3><p class="product-meta">${escapeHtml(variantLabel(variant))}</p>${axisControls(product,variant)||(product.variants.length>1?`<div class="card-variants">${product.variants.slice(0,8).map(v=>`<button class="card-swatch ${String(v.sourceId)===String(variant.sourceId)?'active':''}" style="--swatch:${colorFor(variantLabel(v))}" data-card-variant="${v.sourceId}" data-product="${escapeAttr(product.id)}"></button>`).join('')}</div>`:'')}<div class="product-bottom"><span class="price-stack"><strong class="product-price">${money(variant.wholesalePrice)}</strong>${variant.retailPrice>variant.wholesalePrice?`<del>${money(variant.retailPrice)}</del>`:''}</span><button class="quick-add" data-add="${escapeAttr(product.id)}" data-source="${variant.sourceId}">В корзину</button></div></div></article>`}
function detailTemplate(product,variant){variant=axisVariant(product,variant);const images=(variant.images?.length?variant.images:[variant.primaryImage]).filter(Boolean);return`<article class="detail" data-detail-id="${escapeAttr(product.id)}"><div class="gallery"><img class="gallery-main" id="galleryMain" src="${escapeAttr(imageUrl(images[0]))}" alt="${escapeAttr(stripModel(product.name))}"><div class="thumbnails">${images.map((src,i)=>`<button class="thumbnail ${i===0?'active':''}" data-image="${escapeAttr(imageUrl(src))}"><img src="${escapeAttr(imageUrl(src))}" alt="Фото ${i+1}"></button>`).join('')}</div></div><div class="detail-copy"><p class="eyebrow">${escapeHtml(product.category)}</p><h2>${escapeHtml(stripModel(product.name))}</h2><div class="detail-prices"><strong class="detail-price">${money(variant.wholesalePrice)}</strong>${variant.retailPrice>variant.wholesalePrice?`<span>Розничная цена <del>${money(variant.retailPrice)}</del></span>`:''}</div>${axisControls(product,variant)||(product.variants.length>1?`<div class="variant-options">${product.variants.map(v=>`<button class="variant-option ${String(v.sourceId)===String(variant.sourceId)?'active':''}" data-variant="${v.sourceId}">${escapeHtml(variantLabel(v))}</button>`).join('')}</div>`:'')}<div class="variant-section"><div class="variant-label"><strong>Характеристики и размеры</strong><span>арт. ${variant.sourceId}</span></div><p class="specs">${escapeHtml(variant.specs)}</p></div><div class="detail-actions"><button class="primary-button" data-add="${escapeAttr(product.id)}" data-source="${variant.sourceId}">Добавить в корзину</button><button class="share-button" data-share-product="${escapeAttr(product.id)}" data-source="${variant.sourceId}">Поделиться</button></div></div></article>`}
function renderCart(){const rows=cartRows();els.cartItems.innerHTML=rows.length?`<div class="cart-list">${rows.map(({product,variant,key,qty})=>`<article class="cart-item"><img src="${escapeAttr(imageUrl(variant.primaryImage))}" alt=""><div><h3>${escapeHtml(stripModel(product.name))}</h3><p>${escapeHtml(variantLabel(variant))}<br>арт. ${variant.sourceId}</p><div class="qty"><button data-qty="${key}" data-delta="-1">−</button><span>${qty}</span><button data-qty="${key}" data-delta="1">+</button></div></div><div><strong>${money(variant.wholesalePrice*qty)}</strong><button class="remove" data-remove="${key}">Удалить</button></div></article>`).join('')}</div>`:'<div class="cart-empty"><h3>Корзина пока пуста</h3></div>';const total=rows.reduce((sum,row)=>sum+row.variant.wholesalePrice*row.qty,0),share=cartShareUrl(),plain=orderText(rows,total)+'\n'+share,message=encodeURIComponent(plain),subject=encodeURIComponent('Заказ с сайта FORMA HOME');els.cartFooter.innerHTML=rows.length?`<div class="cart-total"><span>Итого</span><span>${money(total)}</span></div><div class="checkout-actions"><a class="primary-button" href="https://wa.me/?text=${message}" target="_blank" rel="noopener">Заказать в WhatsApp</a><a class="messenger-button" href="https://t.me/share/url?url=${encodeURIComponent(share)}&text=${encodeURIComponent(orderText(rows,total))}" target="_blank" rel="noopener">Заказать в Telegram</a><a class="messenger-button" href="https://max.ru/:share?text=${message}" target="_blank" rel="noopener">Заказать в MAX</a><a class="messenger-button" href="mailto:?subject=${subject}&body=${message}">Заказать по электронной почте</a><button class="share-button" data-share-cart>Поделиться корзиной</button></div>`:''}
document.addEventListener('click',async event=>{const button=event.target.closest('[data-axis]');if(!button)return;event.stopPropagation();const product=state.products.find(p=>p.id===button.dataset.product),match=selectAxis(product,button.dataset.axis,button.dataset.axisValue);if(!match)return;if(button.closest('[data-detail-id]')){const full=await getFullProduct(product);full._selected=match.sourceId;els.detail.innerHTML=detailTemplate(full,currentVariant(full,match.sourceId))}else renderCatalog()},true);
