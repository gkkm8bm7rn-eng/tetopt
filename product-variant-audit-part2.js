window.__FORMA_VARIANT_AUDIT_SOURCE__+="roupProducts, source = \"audit\") {\n      const chosenColors = chooseColors(groupProducts);\n      const raw = groupProducts.map((product, index) => {\n        const color = chosenColors[index];\n        const material = materialFor(product);\n        const finish = finishFor(product);\n        return {\n          product,\n          color,\n          material,\n          finish,\n          baseLabel: color?.label || \"Вариант\",\n          css: color?.css || \"#d8d3ca\"\n        };\n      });\n\n      const colorCounts = new Map();\n      raw.forEach(item => colorCounts.set(item.baseLabel, (colorCounts.get(item.baseLabel) || 0) + 1));\n\n      raw.forEach(item => {\n        const details = [];\n        if ((colorCounts.get(item.baseLabel) || 0) > 1 && item.material) details.push(item.material);\n        if (item.finish) details.push(item.finish.toLowerCase());\n        item.label = details.length ? `${item.baseLabel}, ${details.join(\", \")}` : item.baseLabel;\n      });\n\n      const byVariant = new Map();\n      for (const item of raw) {\n        const key = normalize(item.label);\n        if (!byVariant.has(key)) byVariant.set(key, []);\n        byVariant.get(key).push(item);\n      }\n\n      const variants = [];\n      const duplicates = [];\n      for (const items of byVariant.values()) {\n        items.sort((a, b) =>\n          Number(knownDuplicateHiddenIds.has(productId(a.product))) - Number(knownDuplicateHiddenIds.has(productId(b.product))) ||\n          (Number(a.product.wholesalePrice) || Infinity) - (Number(b.product.wholesalePrice) || Infinity) ||\n          productId(a.product) - productId(b.product)\n        );\n        const representative = items[0];\n        variants.push({\n          id: productId(representative.product),\n          label: representative.label,\n          buttonLabel: representative.label,\n          css: representative.css\n        });\n        if (items.length > 1) duplicates.push(items.map(item => productId(item.product)));\n      }\n\n      variants.sort((a, b) => a.id - b.id);\n      const uniqueLabels = new Set(variants.map(variant => normalize(variant.label)));\n      if (variants.length < 2 || uniqueLabels.size < 2) {\n        return { group: null, duplicates, source };\n      }\n\n      const colorLabels = variants.map(variant => normalize(variant.label.split(\",\")[0]));\n      const display = new Set(colorLabels).size < variants.length || variants.some(variant => /матов|глянц|подушк/i.test(variant.label))\n        ? \"text\"\n        : \"swatch\";\n\n      return {\n        group: {\n          name,\n          primaryId: variants[0].id,\n          label: display === \"text\" ? \"Вариант\" : \"Цвет\",\n          ...(display === \"text\" ? { display: \"text\" } : {}),\n          variants\n        },\n        duplicates,\n        source\n      };\n    }\n\n    const explicitGroups = Array.isArray(window.PRODUCT_EXPLICIT_VARIANT_GROUPS)\n      ? [...window.PRODUCT_EXPLICIT_VARIANT_GROUPS]\n      : [];\n    const explicitIds = new Set(explicitGroups.flatMap(group => (group.variants || []).map(variant => Number(variant.id))));\n    const dualIds = new Set((window.PRODUCT_DUAL_VARIANT_GROUPS || []).flatMap(group => [\n      ...(group.ids || []),\n      ...(group.variants || []).map(variant => variant.id)\n    ]).map(Number));\n    const reservedIds = new Set([...explicitIds, ...dualIds]);\n\n    const rawColorGroups = Array.isArray(window.PRODUCT_COLOR_GROUPS)\n      ? window.PRODUCT_COLOR_GROUPS\n      : [];\n    const currentDuplicateGroups = Array.isArray(window.PRODUCT_DUPLICATE_GROUPS)\n      ? [...window.PRODUCT_DUPLICATE_GROUPS]\n      : [];\n    const duplicateAdjacency = new Map();\n    const currentDuplicateIds = new Set();\n    knownDuplicateHiddenIds = new Set(\n      currentDuplicateGroups.flatMap(group => (group.ids || []).map(Number).filter(Number.isFinite).slice(1))\n    );\n    for (const duplicateGroup of currentDuplicateGroups) {\n      const ids = [...new Set((duplicateGroup.ids || []).map(Number).filter(Number.isFinite))];\n      ids.forEach(id => {\n        currentDuplicateIds.add(id);\n        if (!duplicateAdjacency.has(id)) duplicateAdjacency.set(id, new Set());\n        ids.forEach(otherId => duplicateAdjacency.get(id).add(otherId));\n      });\n    }\n\n    const mergedColorMap = new Map();\n    rawColorGroups.forEach((group, groupIndex) => {\n      const initialIds = [...new Set((group.ids || []).map(Number).filter(Number.isFinite))];\n      const expanded = new Set(initialIds);\n      initialIds.forEach(id => (duplicateAdjacency.get(id) || []).forEach(otherId => expanded.add(otherId)));\n      const groupProducts = [...expanded].map(id => byId.get(id)).filter(Boolean);\n      const families = [...new Set(groupProducts.map(product => packageFreeName(product.name)).filter(Boolean))];\n      const reserved = [...expanded].some(id => reservedIds.has(id));\n      const key = !reserved && families.length === 1 ? `family:${families[0]}` : `group:${groupIndex}`;\n      if (!mergedColorMap.has(key)) mergedColorMap.set(key, { name: group.name, ids: [] });\n      const target = mergedColorMap.get(key);\n      target.ids.push(...expanded);\n      target.ids = [...new Set(target.ids)];\n    });\n    const currentColorGroups = [...mergedColorMap.values()];\n\n    const duplicateAdditions = [];\n    const converted = [];\n    const removedNonColor = [];\n    const retainedColorGroups = [];\n    const coveredIds = new Set([...reservedIds, ...currentDuplicateIds]);\n\n    for (const group of currentColorGroups) {\n      const ids = [...new Set((group.ids || []).map(Number).filter(Number.isFinite))];\n      const groupProducts = ids.map(id => byId.get(id)).filter(Boolean);\n      ids.forEach(id => coveredIds.add(id));\n\n      if (groupProducts.length < 2 || ids.some(id => reservedIds.has(id))) {\n        retainedColorGroups.push(group);\n        continue;\n      }\n\n      const legacyLabels = new Set(groupProducts.map(product => normalize(legacyColor(product))));\n      const result = buildExplicitGroup(group.name || groupProducts[0].name || \"Варианты товара\", groupProducts, \"existing-color-group\");\n      const robustBaseLabels = new Set((result.group?.variants || []).map(variant => normalize(String(variant.label || \"\").split(\",\")[0])));\n      const legacyKey = [...legacyLabels].sort().join(\"|\");\n      const robustKey = [...robustBaseLabels].sort().join(\"|\");\n      const hasCollapsedDuplicates = Boolean(result.group) && groupProducts.length > result.group.variants.length;\n      const needsExplicitCorrection = Boolean(result.group) && (\n        legacyLabels.size < robustBaseLabels.size ||\n        legacyLabels.size < 2 ||\n        hasCollapsedDuplicates\n      );\n\n      if (result.group && !needsExplicitCorrection) {\n        retainedColorGroups.push(group);\n        continue;\n      }\n\n      result.duplicates.forEach(idsToHide => mergeDuplicateGroup(duplicateAdditions, `${group.name || \"Товар\"} — дубль варианта`, idsToHide));\n\n      if (result.group) {\n        explicitGroups.push(result.group);\n        result.group.variants.forEach(variant => reservedIds.add(Number(variant.id)));\n        converted.push({ name: result.group.name, ids: result.group.variants.map(variant => variant.id), display: result.group.display || \"swatch\" });\n      } else {\n        const selected = chooseColors(groupProducts).map(item => item?.label || \"\");\n        const sameSelected = new Set(selected.map(normalize)).size <= 1;\n        const dimensions = new Set(groupProducts.map(dimensionSignature));\n        const configurations = new Set(groupProducts.map(product => finishFor(product)).filter(Boolean));\n        if (sameSelected && dimensions.size <= 1 && configurations.size <= 1) {\n          mergeDuplicateGroup(duplicateAdditions, `${group.name || \"Товар\"} — одинаковый вариант`, ids);\n        } else {\n          removedNonColor.push({ name: group.name || groupProducts[0].name, ids });\n        }\n      }\n    }\n\n    const blockedFamilies = new Set();\n    products.forEach((product, index) => {\n      const id = productId(product, index);\n      if (coveredIds.has(id) || reservedIds.has(id) || currentDuplicateIds.has(id)) {\n        blockedFamilies.add(packageFreeName(product.name));\n      }\n    });\n\n    const discoveryBuckets = new Map();\n    products.forEach((product, index) => {\n      const id = productId(product, index);\n      const family = packageFreeName(product.name);\n      if (!family || blockedFamilies.has(family)) return;\n      if (coveredIds.has(id) || reservedIds.has(id) || currentDuplicateIds.has(id)) return;\n      if (!discoveryBuckets.has(family)) discoveryBuckets.set(family, []);\n      discoveryBuckets.get(family).push(product);\n    });\n\n    const discovered = [];\n    for (const [family, familyProducts] of discoveryBuckets.entries()) {\n      if (familyProducts.length < 2) continue;\n      const structures = new Map();\n      for (const product of familyProducts) {\n        const signature = structuralSignature(product);\n        if (!structures.has(signature)) structures.set(signature, []);\n        structures.get(signature).push(product);\n      }\n\n      for (const structurallySame of structures.values()) {\n        if (structurallySame.length < 2) continue;\n        const result = buildExplicitGroup(structurallySame[0].name || family, structurallySame, \"discovered-family\");\n        result.duplicates.forEach(idsToHide => mergeDuplicateGroup(duplicateAdditions, `${structurallySame[0].name} — дубль варианта`, idsToHide));\n        if (!result.group) continue;\n        const allIds = structurallySame.map(product => productId(product));\n        if (allIds.some(id => reservedIds.has(id))) continue;\n        explicitGroups.push(result.group);\n        allIds.forEach(id => reservedIds.add(id));\n        discovered.push({ name: result.group.name, ids: result.group.variants.map(variant => variant.id), display: result.group.display || \"swatch\" });\n      }\n    }\n\n    const explicitSeen = new Set();\n    window.PRODUCT_EXPLICIT_VARIANT_GROUPS = explicitGroups.filter(group => {\n      const ids = (group.variants || []).map(variant => Number(variant.id)).filter(Number.isFinite);\n      const key = uniqueGroupKey(ids);\n      if (ids.length < 2 || explicitSeen.has(key)) return false;\n      explicitSeen.add(key);\n      return true;\n    });\n\n    const convertedIds = new Set(\n      window.PRODUCT_EXPLICIT_VARIANT_GROUPS.flatMap(group => (group.variants || []).map(variant => Number(variant.id)))\n    );\n    window.PRODUCT_COLOR_GROUPS = retainedColorGroups.filter(group =>\n      !(group.ids || []).some(id => convertedIds.has(Number(id)))\n    );\n\n    const duplicateMerged = [...currentDuplicateGroups];\n    for (const group of duplicateAdditions) mergeDuplicateGroup(duplicateMerged, group.name, group.ids);\n    window.PRODUCT_DUPLICATE_GROUPS = duplicateMerged;\n\n    window.__FORMA_FULL_VARIANT_AUDIT__ = {\n      enabled: true,\n      version: 1,\n      rule: \"group-aware color selection from specs; packaging ignored; dimensions preserved\",\n      sourceColorGroupCount: rawColorGroups.length,\n      mergedColorGroupCount: currentColorGroups.length,\n      convertedExistingGroups: converted,\n      convertedExistingCount: converted.length,\n      discoveredGroups: discovered,\n      discoveredCount: discovered.length,\n      duplicateGroupsAdded: duplicateAdditions.map(group => ({ name: group.name, ids: group.ids })),\n      duplicateGroupsAddedCount: duplicateAdditions.length,\n      removedNonColorGroups: removedNonColor,\n      removedNonColorCount: removedNonColor.length,\n      explicitGroupCount: window.PRODUCT_EXPLICIT_VARIANT_GROUPS.length,\n      remainingColorGroupCount: window.PRODUCT_COLOR_GROUPS.length\n    };\n  }\n\n  document.write = function patchedWrite(...parts) {\n    let html = parts.join(\"\");\n    if (typeof html === \"string\" && html.includes(\"</body>\")) {\n      html = html.replace(\"</body>\", `<script>(${runtime.toString()})();<\\/script></body>`);\n    }\n    return originalWrite(html);\n  };\n})();\n";
(0,eval)(window.__FORMA_VARIANT_AUDIT_SOURCE__);
delete window.__FORMA_VARIANT_AUDIT_SOURCE__;
